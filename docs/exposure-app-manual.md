# 露出アシスト — 操作マニュアル（アプリ内表示用）

この文書は**2つの役割**を持つ。

1. **§0**：アプリ内へのマニュアル組み込み仕様（Claude Code 向け。UI 仕様書の追補として読む）
2. **§1 以降**：マニュアル本文の原稿（ユーザーが読む文章そのもの）

**§1 以降の本文がこの文書の正典。** `index.html` に埋め込む HTML はこの写しである。
本文を変更するときは必ずこちらを先に直し、`index.html` に反映する（`docs/MAINTENANCE.md` §7 の手順）。

---

# §0 組み込み仕様

## 0.1 設置場所

**ヘッダー右端の `?` ボタンから開く全画面シート。** タブにしない。別ページにしない。

| 判断 | 理由 |
| --- | --- |
| 4つ目のタブにしない | 固定結果パネルと衝突する。マニュアルの下に結果パネルは不要。320px で4タブは狭い |
| `help.html` に分けない | SW のプリキャッシュ対象が増え、アプリから離脱して再初期化が走る |
| 全画面シート | 読む間は計算画面を隠してよい。原則2は計算画面に対する原則であり、モーダルには適用しない |

## 0.2 マークアップの置き場所

本文は **`index.html` 内の静的 HTML** とする。`<body>` の**末尾**、アプリ本体の後に置く。

```html
<div id="manual" class="sheet" hidden role="dialog" aria-modal="true" aria-labelledby="manual-title">
  <header class="sheet-head">
    <h2 id="manual-title">使い方</h2>
    <button type="button" class="sheet-close" aria-label="閉じる">…</button>
  </header>
  <nav class="manual-index" aria-label="目次">…</nav>
  <div class="manual-body">
    <section id="help-intro">…</section>
    <section id="help-scene">…</section>
    …
  </div>
</div>
```

- JS の文字列テンプレートにしない。日本語の長文を JS 内に持つと編集と校正が困難になる
- `index.html` の**先頭側（アプリ骨格）は読みやすさを保つ**。だから末尾に置く
- 追加の JS は開閉制御のみ。本文の生成処理を書かない

## 0.3 開閉の挙動

| 項目 | 仕様 |
| --- | --- |
| 開く | ヘッダーの `?` ボタン、または警告の `?` ボタン |
| 閉じる | 右上の閉じるボタン / `Esc` キー / 背景のスワイプダウン |
| アニメーション | 下から 200ms / ease-out で `transform: translateY()`。`prefers-reduced-motion` では即時 |
| フォーカス | 開いたら閉じるボタンへ移動。シート内でフォーカストラップ。閉じたら呼び出し元へ戻す |
| スクロール | シート内のみスクロール。背後の body は `overflow: hidden` で固定 |
| 状態 | `state.ui.manual = null | 'help-xxxx'`。`setState` 経由で開閉する（再描画経路を増やさない） |
| 履歴 | `history.pushState` を使わない。ブラウザバックで閉じる挙動は実装しない（PWA でタブ状態と混ざる） |

## 0.4 目次（左上固定のメニュートレイ）

**シート最上部に置かない。** 本文を読み進めると画面外へ消え、別セクションへ移るのに
最上部まで戻る往復が必要になる（実機で確認済み）。**左上に固定したトレイ**にする。

| 項目 | 仕様 |
| --- | --- |
| 配置 | シート左上に固定。本文をスクロールしても追従する |
| 開く | ヘッダー左端のアイコン押下、または**シート内を右へスワイプ** |
| 閉じる | 同じアイコンを再度押下、または**左へスワイプ** |
| 項目押下 | 該当セクションへスクロール。**ジャンプ後もトレイは開いたまま**（自動で閉じない） |
| 重なり | 本文の**上に被さる**。本文の幅は狭めない（見づらければユーザーが閉じる） |
| 本文 | トレイ展開中も本文はスクロール可能。本文側の `overflow` は止めない |
| モーダル性 | トレイもシートも背後の計算画面を見せない |
| アニメーション | `transform: translateX()` 200ms / ease-out。`prefers-reduced-motion` では即時 |
| タップ領域 | アイコンは 44px 以上。左上固定なので片手の親指が届く |

- 状態は `state.ui.manualTray: boolean` で駆動し、`setState` 経由のみ。DOM に状態を持たせない
- **スワイプの開始位置を画面左端に限定しない。** 左端から始まるスワイプは Safari の戻るジェスチャと
  競合する（ブラウザのタブで開いているときに発動する）。シート内のどこからでも効くようにする
  ——シート内に横スワイプで動く部品は無いので競合しない
- **縦スワイプ（シートを閉じる）との軸判別：** `|dx| > |dy| × 1.5` かつ `|dx| > 40px` なら横スワイプ。
  それ以外は既存の縦スワイプ判定に渡す
- スワイプ中の追従表示はしない（閾値を超えた時点で開閉する）。アニメーションはアイコン操作と同じ
- **閉じているときトレイ内のリンクは Tab で辿れないようにする**（`tabindex="-1"`）。
  開いているときはフォーカストラップの一部として辿れる
- スクロールは `scrollIntoView({ behavior: 'smooth', block: 'start' })`。
  `prefers-reduced-motion` では `behavior: 'auto'`

**アンカー指定で開いた場合は目次を飛ばして該当セクションを最上部に表示する。
このときトレイは閉じた状態で開く。**

## 0.5 警告からの導線（重要）

`advisor.js` の戻り値に `helpId` を追加する。

```js
// 変更前
{ level: 'warn', message: '…' }

// 変更後
{ level: 'warn', message: '…', helpId: 'help-sync-wall' }
```

- **`helpId` は必須フィールド**とする。全ての警告が必ずセクションを1つ持つ
- `advisor.js` は純粋関数のまま。文字列 ID を返すだけで DOM に触らない
- `ui.js` は `helpId` があれば警告の右端に `?` ボタンを描画し、押されたら `setState({ ui: { manual: helpId } })`

### 警告 → セクション 対応表

**この表は実装（`advisor.js` / `compute.js`）を正典として作られている。**
警告を増やしたら、まず実装に `helpId` を付け、`js/scenes.js` の `HELP` に値を追加し、この表を更新する。
`HELP` が唯一の定義場所で、文字列を実装に直書きしない。

| 警告（実装の文言） | レベル | 発生源 | `helpId` |
| --- | --- | --- | --- |
| 手ブレしやすい速度です（限界より◯段遅い） | warn | `advisor.shakeWarning` | `help-warn-shake` |
| 明るすぎます。◯段の減光が必要です。ND◯+ND◯（◯段）を装着してください | alert | `advisor.overBrightWarning` | `help-nd` |
| 明るすぎます。ISO を◯段下げきれません（拡張下限） | alert | `advisor.isoFloorWarning` | `help-nd` |
| 閃光時間◯ ではこの被写体は止まりません | warn | `advisor.freezeWarning` | `help-flash-duration` |
| ISO を拡張下限まで下げれば ND ◯枚で済みます | info | `advisor.ndCountWarnings` | `help-nd` |
| ND を◯枚重ねます。ケラレ・色被りに注意 | warn | `advisor.ndCountWarnings` | `help-nd` |
| HSS経路は SS上限のため背景は◯段までです | warn | `advisor.daylightSync` | `help-sync-wall` |
| 空が飛ぶ可能性があります（◯の目安 ◯段）＋選ぶべき背景段数 | warn | `advisor.blowoutWarning` | `help-blowout` |
| 光量が◯段足りません。開放を明るく、または三脚が必要です | alert | `compute.clampAmbient` | `help-warn-light-short` |
| 暗すぎます。SS は 30″ が限界で◯段 足りません | alert | `compute.clampAmbient` | `help-warn-light-short` |
| 被写体が流れます。1/◯ 以上が必要（◯段不足） | warn | `compute.clampAmbient` | `help-warn-motion` |
| 被写体が流れます。1/◯ 以上にしてください | warn | `compute.clampAmbient` | `help-warn-motion` |
| 回折で解像が落ち始めます | info | `compute.clampAmbient` | `help-warn-diffraction` |
| ノイズが目立ち始める領域です | info | `compute.clampAmbient` | `help-warn-high-iso` |
| 三脚推奨（SS 1/2秒以上） | info | `compute.clampAmbient` | `help-warn-tripod` |
| 発光量◯ では◯段 強すぎます（距離・F・ISO の3案） | warn | `compute.resolveFlashSide` | `help-power-mode` |
| 発光量◯ では◯段 足りません（距離・F・ISO の3案） | warn | `compute.resolveFlashSide` | `help-power-mode` |
| この機材では届きません。フル発光でも◯段 足りません（到達◯m／設定◯m） | alert | `compute.resolveFlashSide` | `help-warn-flash-short` |
| 上限◯ では◯段 足りません（上限を◯に上げる〈発光量◯で撮れます〉・距離・ISO。ND は出さない） | alert | `compute.resolveFlashSide` | `help-power-ceiling` |
| 最小発光量◯ でも◯段 強すぎます | warn | `compute.resolveFlashSide` | `help-warn-flash-strong` |
| ◯軸 が◯段 足りません（計算タブのクランプ） | alert | `compute.manualShort` | `help-calc-clamp` |
| 明るすぎます（◯段超過）。ND◯+ND◯（◯段）を装着／…（計算タブのクランプ） | alert | `compute.manualShort` | `help-calc-clamp` |
| 同調速度の壁を越えられません。F を絞ってください | alert | `compute.computeDaylight` | `help-sync-wall` |
| 後幕シンクロ推奨。被写体が動くと残像が出ます | info | `compute.computeSlow` | `help-slow-sync` |
| フル発光に近く、連写ではチャージが追いつきません | info | `compute.computeSlow` | `help-warn-recycle` |
| この設定で撮れます（警告なしのとき） | info | `compute` | `help-warnings` |

**`helpId` は例外なく必須。** 「この設定で撮れます」にも付ける（当初は `helpId` なしと決めていたが、
必須フィールドという規則と矛盾するため実装に合わせて改めた。遷移先は §14 警告の読み方）。

> **未校正プロファイル**は警告ではなく結果パネルの**バッジ**で示すため、この表には含めない
> （バッジからの導線は `HELP_LINKS.calibration` → `help-calibration`）。

> **「写真から測る」の注記**（EXIF なし・撮影情報の不足・ナイトモード・露出補正・別レンズ・HEIC）も
> この表に含めない。`compute()` の警告ではなく**測定 UI 側のその場の表示**であり、
> `advisor.js` / `compute.js` からは発生しない。導線は `HELP_LINKS.lightmeter` →
> `help-lightmeter`（§3「写真から測る」）の1本にまとめる。
>
> **`HELP` に足さないこと。** テスト #33 は「`HELP` の全値がいずれかの警告から発生する」ことを
> 検証するので、警告でない導線を `HELP` に入れると永久に赤くなる。

**ストロボ側とアンビエント側の光量不足は別のセクションに分ける。**
`help-warn-flash-short` はストロボ側（距離・発光量・NDで解決）、
`help-warn-light-short` はアンビエント側（ISO上限・レンズ開放でも足りない）。対処が違うため。

**計算仕様 §9 にあるが未実装の警告**（実装したらこの表に追加する）：
`ND 装着でストロボ到達距離が半減以上`（→ `help-flash-basics`）、
`HSS 損失 > 4段`（→ `help-sync-wall`）、
`同調速度超過で HSS 非対応/off`（→ `help-sync-wall`。現状は「壁を越えられません」で代替）。

## 0.6 追加する受け入れテスト

`tests.html` に1件追加する（テスト #33）。

| # | 入力 | 期待 |
| --- | --- | --- |
| 33 | `advisor.js` / `compute.js` が返し得る全警告を網羅的に発生させる | すべての警告が空でない `helpId` を持ち、その値が §0.5 の表に存在すること。さらに `HELP` の全値が少なくとも1回は発生すること（到達不能な分岐を検出する） |

既存テストは `level` と `message` を検証しているため、フィールド追加では赤にならない。

**`index.html` 側のアンカー実在は `tests.html` では検証できない**（DOM を見ないため）。
`bash tools/check-help-anchors` で機械的に検査する（`MAINTENANCE.md` §8）。

## 0.7 スタイル

- 本文は `body`（15/400、行高 1.6）。見出しは `title`（15/600）
- コードや数値の例示は `--font-num` を使う
- 装飾を足さない。イラスト・図版は入れない（PNG を増やさない）
- 表は使ってよいが、320px で横スクロールさせない。2列までに収める
- リンクは同一シート内のセクション参照のみ。**外部 URL を1つも書かない**

---

# §1 このアプリについて

## できること

いまの光の状態と「どう撮りたいか」を選ぶと、F値・シャッター速度・ISO・ストロボの発光量を計算します。
数字を出すだけでなく、**その設定で何が問題になるか**（手ブレする、光が届かない、絞りきれない）を段数付きで教えます。

電波がなくても動きます。すべての計算は端末の中だけで行われ、通信は一切しません。

## 3つの画面

| タブ | 使う場面 |
| --- | --- |
| **かんたん** | シーンと撮りたい雰囲気を選んで、設定を出してもらう。通常はこれだけで足ります |
| **計算** | F値・SS・ISO のうち2つを自分で決めて、残り1つを出す。等価露出の一覧も見られます |
| **設定** | 手持ちの機材を登録する。ストロボの校正もここ |

## 結果パネルの高さを変える

画面下の結果パネルは、上端の**横棒（ハンドル）**を上下にスワイプすると3段階に変わります。
タップすると標準に戻ります。

| 高さ | 向いている場面 |
| --- | --- |
| 大 | EVルーラーとトラックを見ながら追い込む |
| 標準 | 通常 |
| 小 | 数値1行だけ。シーンや距離をたくさん触りたいとき |

**小さくしても F値・SS・ISO（ストロボONなら発光量も）は必ず見えています。**
警告が出ているときは行末にアイコンが残るので、気づかずに撮ることはありません。

選んだ高さは記憶され、次に開いたときも同じです。
横持ちで画面が低いときは「大」は選べません（入力欄が潰れるため）。

## 数値の性格について

このアプリが出す数字には**2種類**あります。

**厳密なもの** — 露出計算そのもの（F値・SS・ISO の関係、段数の差）。これは数式で決まるので誤差はありません。

**目安のもの** — シーンの明るさ（EV）、ストロボの出力から推定したガイドナンバー、HSS の光量ロス、モディファイアの減光、閃光時間。これらは機材や状況で変わります。**±1段程度はズレる前提**で使ってください。

目安の項目は画面上に「推定値」と表示されます。ストロボについては§12の校正で実測に合わせられます。

---

# §2 <a id="help-setup"></a>最初にやること

初回起動時に3画面の機材登録が出ます。**全部「あとで」で飛ばしても使えます。**
急いでいるときは飛ばして、落ち着いたときに設定タブで入れてください。

| 画面 | 入れるもの | 分からないとき |
| --- | --- | --- |
| カメラ | 同調速度 / SS の最速 / ベースISO / 実用ISO上限 | 既定値のまま |
| レンズ | 開放F値 / 常用焦点距離 / 手ブレ補正段数 | 開放F値だけ合わせれば十分 |
| ストロボ | 出力(Ws) / HSS対応 / モディファイア | 出力だけ合わせる |

モディファイアの選択肢は **標準リフレクター / AK-R1 ドームディフューザー / オクタ30cm /
オクタ60cm / オクタ90cm** です。減光段数は 0／1.5／1.5／2.0／2.5段の**推定値**で、
オクタは径が大きいほど拡散して減光が増えます。製品差が大きいので、よく使う組み合わせは
§12 の校正で実測に合わせてください。

**手ブレ補正段数は、公称値の6〜7割**を入れるのが実用的です。「5段補正」と書かれていても、
現場で実際に効くのは3段程度です。公称値をそのまま入れると、警告が出るべき場面で出なくなります。

設定はいつでも変更でき、変更するとその場で計算し直されます。

---

# §3 <a id="help-scene"></a>シーンの選び方

## 選んでから微調整する

シーンのタイルを選ぶと、そのシーンの明るさ（EV）が入ります。**この値は±1段程度の目安**です。
実際の光に合わせて、下の微調整スライダーで動かしてください。1/3段刻みで±2段まで動きます。

| 迷ったら | 選ぶもの |
| --- | --- |
| 影がくっきり出ている | 快晴・順光（EV15） |
| 影の輪郭がやわらかい | 晴れ・薄曇り（EV14） |
| 影が出ていない | 曇り（EV13） |
| 建物や木の陰にいる | 厚い曇り／建物の日陰（EV12） |

**影の状態を見るのが一番確実です。** 空の見た目より正確に判断できます。

## 微調整の使い方

撮った結果が明るすぎた／暗すぎたときは、その差ぶんだけスライダーを動かします。
1段暗かったなら +1.0、1/3段だけ明るかったなら −0.3 です。

同じ場所で撮り続けるなら、最初の1枚で微調整を決めてしまえば以降は合います。

## <a id="help-lightmeter"></a>写真から測る（スマホを露出計にする）

シーンのタイルは±1段の目安ですが、**スマホで1枚撮ればその場の明るさを実測できます。**
スマホのカメラは自動露出でF値・SS・ISOを決め、それを写真に記録しています。
その3つから逆算すると、シーンのEVが求まります。**これは露出計そのものの原理です。**

1. 測りたい方向にスマホのカメラを向けて1枚撮る
2. 「写真を選ぶ」でその写真を選ぶ
3. 「EV 14.1 を測定しました」と出たら「使う」を押す

使うとシーンのタイルの選択が外れ、「実測 EV 14.1 を使用中」になります。
**微調整スライダーはそのまま使えます。** 実測値からの補正として働きます。

**日中シンクロではシーンEVが背景の明るさを直接決めます。** ここが実測になると、
狙った背景の明るさになる確率が上がります。

### 写真は端末から出ません

**写真は端末内で読み取るだけで、どこにも送信されません。位置情報は読み取りません。**
このアプリは通信をしない作りなので、これは事実としてそう書けます。
写真そのものも保存しません。取り出すのは明るさの数値だけです。

### 露出補正をかけないでください

スマホで露出補正をかけて撮ると、その分ずれます。アプリは補正の値を読んで計算に入れますが、
**補正がかかっていると自動露出の狙いから外れているので、精度は落ちます。**
補正がかかった写真を選ぶと注記が出ます。**補正を 0 に戻してから撮り直すのが確実です。**

ナイトモードは切ってください。多重露光なので記録された値の意味が変わります。
HDR は切ってもよいですが、**切るより「いつも同じ設定で使う」ほうが実用的です。**
ずれが一定なら、下の校正で丸ごと吸収されます。

### 「推定値（未校正）」と出たら

スマホの自動露出は 18%グレーに合わせるとは限りません。明るい場面ではハイライトを守って
暗めに、暗い場面では持ち上げ気味に決めます。**このずれは機種ごとにほぼ一定です。**

未校正のままでも使えますが、精度は±2段程度です。**設定タブの「スマホ露出計」で
一度校正すると±1/3段まで上がります。** 一度測れば以後ずっと使えます。

| 状態 | 精度 |
| --- | --- |
| 未校正 | ±2段 |
| 校正済み・同じレンズ・似たシーン | **±1/3段** |
| 校正済み・レンズ切替（0.5x / 2x） | ±2/3段 |
| 校正済み・逆光や夜景 | ±1段 |

校正のしかたは、同じシーンをスマホとカメラの両方で測って差を保存するだけです。
**基準はカメラです。** このアプリの目的は「カメラがどう写るか」を当てることなので、
合わせる先はカメラでなければ意味がありません。

iPhone はレンズが複数あります（超広角・主・望遠）。校正したときと大きく違うレンズで
撮った写真を選ぶと注記が出ます。値は出ますが、精度は落ちます。

### 読み取れないとき

| 表示 | 意味 |
| --- | --- |
| この写真には撮影情報がありません | スクリーンショットや加工後に保存し直した写真です。カメラアプリで撮った写真を選んでください |
| 撮影情報が不足しています | F値・SS・ISO のどれかが記録されていません |
| 設定 → カメラ → フォーマット → 互換性優先 に変更すると読み取れます | 写真が HEIC のまま渡されました。通常は iOS が JPEG に変換するので、ここは出ないはずです |

---

# §4 <a id="help-intent"></a>撮りたいものから決める（かんたんタブ）

意図を選ぶと、アプリが「何を優先して何を犠牲にするか」を決めて計算します。

| 意図 | 何を固定するか | 犠牲になるもの |
| --- | --- | --- |
| **背景をぼかす** | F値を開放に固定 | 明るい場所ではSSが速くなりすぎる／暗所ではISOが上がる |
| **動きを止める** | 被写体に応じたSSに固定 | ISOが上がる |
| **風景をくっきり** | F8・ベースISOに固定 | SSが遅くなり三脚が必要になる場合がある |
| **夜・手持ち** | SSを手ブレ限界に固定 | ISOが大きく上がる |
| **日中シンクロ** | §6 参照 | — |
| **スローシンクロ** | §8 参照 | — |

日中シンクロとスローシンクロを選ぶとストロボの入力欄が開きます。
距離の下にある**発光量チップ**（おまかせ / 1/8 / 1/16 / 1/32 / 1/64 / 1/128）で、
発光量をアプリに任せるか自分で決めるかを選べます。詳しくは §10 を読んでください。

## 結果の3案

結果は**主案＋代替2案**で出ます。3案はすべて**同じ明るさになる組み合わせ**です。

| 案 | 性格 |
| --- | --- |
| 主案 | 意図に沿った中庸な設定 |
| ブレを避ける | SSを上げてブレ警告を消す。そのぶんISOが上がる |
| 画質を優先 | ISOを下げる。そのぶんSSが遅くなる |

**ノイズとブレのどちらを取るか**という選択なので、現場の状況で選んでください。
成立しない案は表示されません（3案が常に出るわけではありません）。

---

# §5 <a id="help-flash-basics"></a>ストロボの基本 — 覚えておくと全部が分かる

ストロボを使うとき、**背景と主被写体は別々に決まります。** これがこのアプリの中核です。

| | 効くもの | 効かないもの |
| --- | --- | --- |
| **背景（アンビエント）** | F値・SS・ISO・ND | — |
| **主被写体（ストロボ）** | F値・ISO・距離・発光量・ND | **SS** |

## なぜSSがストロボに効かないのか

ストロボの発光は 1/250 〜 1/9000 秒で終わります。シャッターが開いている時間より短いので、
**SSを変えても被写体に届く光の量は変わりません。**

これを使うと、こういうことができます。

- **SSを速くする** → 背景だけが暗くなる。被写体の明るさはそのまま
- **SSを遅くする** → 背景だけが明るくなる。被写体の明るさはそのまま

つまり **SS は背景の明るさ専用のダイヤル**として使えます。日中シンクロもスローシンクロも、
原理はこれだけです。

> 計算タブでストロボをONにして、SSのトラックを動かしてみてください。
> **発光量のトラックだけが動きません。** これがこの原理そのものです。

## NDは両方に効く

一方、NDフィルターはレンズの前にあるので、**背景とストロボの両方を同じだけ暗くします。**
ND4（2段）を付けたら、ストロボの発光量も2段上げないと被写体が暗くなります。

> NDを足したときの見え方は、**どの値をアプリに任せているか**で変わります。
> ISOやF値を任せているときは、その値がNDのぶんを補うので**発光量は動きません。**
> SSを任せているときだけ発光量も動きます（SSはストロボに効かないため、NDの減光が補われずに残る）。
>
> つまり「**SSを自分で動かすと発光量は動かない。NDに押されてSSが動いたときは発光量も動く**」
> という対比になります。見比べると違いが分かります。

---

# §6 <a id="help-daylight-sync"></a>日中シンクロの手順

明るい屋外で、逆光や影に入った被写体をストロボで起こす撮り方です。

## 入力するもの

1. シーン（＋微調整）
2. 意図で「日中シンクロ」を選ぶ
3. ストロボ：使うプロファイル / モディファイア / **ストロボ→被写体の距離**
4. 背景の明るさ：既定は −1段（背景が1段暗く写る）

**距離はカメラからではなくストロボから被写体まで**です。オフカメラで使うと両者は別物になります。

## 背景の明るさの目安

| 設定 | 仕上がり |
| --- | --- |
| 0段 | 背景も被写体も適正。自然だが平坦になりやすい |
| **−1段** | 背景が少し落ちて被写体が浮く。最も使いやすい |
| −2段 | 背景がしっかり暗い。ドラマチック |
| −3段 | 背景がほぼ黒。夜のように見える |
| +1段 | 背景を明るく飛ばす。ハイキー |

## 画面の読み方

一番上に **「同調速度の壁：+○段」** が出ます。これが日中シンクロで最も重要な数字です。
0段なら何も考えず撮れます。正の数なら§7の対処が必要です。

その下にアンビエント側とストロボ側が並びます。

```
┌─ 背景 ─────────────┐  ┌─ 主被写体 ────────┐
│ F2.8  1/250  ISO200 │  │ 発光量 1/2  FEC −0.1│
│ ND2 + ND16 装着      │  │ 到達 5.0m（設定 3.0m）│
│ 背景は −1.0 段       │  │ 閃光時間 1/400       │
└────────────────────┘  └────────────────────┘
```

**到達距離が設定距離より大きければ光は届いています。** 小さければ足りません。

---

# §7 <a id="help-sync-wall"></a>同調速度の壁 — 何が起きているか

## 壁の正体

ストロボは**同調速度より速いシャッターでは使えません。** シャッター幕が写り込んで、
画面の一部に黒い帯が出ます。多くのカメラで 1/200〜1/250 秒です。

ところが晴れた屋外は明るすぎます。**絞りを開けたいのに、SSを上げられない。** これが壁です。

## 具体例

快晴（EV15）、ベースISO200、同調速度 1/250 のカメラで考えます。

このときの適正F値は **F16** です。しかし背景をぼかすために **F2.8** で撮りたい。
F16 と F2.8 の差は **5段**。これが「同調速度の壁：+5.0段」の意味です。

## 越える方法は2つ

アプリは**両方を計算して並べます。自動で選びません。**

| 経路 | やること | ストロボへの影響 |
| --- | --- | --- |
| **ND経路** | NDで5段減光し、SSは1/250に留める | ストロボも5段減光される |
| **HSS経路** | HSSに切り替えてSSを1/8000まで上げる | 基準損失2段＋超過5段＝**7段**の損失 |

先ほどの例（200Ws・標準リフレクター・ISO200）で比較すると、

```
            SS       ND          発光量   到達距離
ND経路     1/250    ND2+ND16     1/2      5.0m
HSS経路    1/8000   なし         1/1      2.5m   ← 2段不利
```

**ND経路のほうが2段有利で、届く距離が2倍になります。**

## どちらを選ぶか

| ND経路が向く | HSS経路が向く |
| --- | --- |
| 被写体との距離を取りたい | 動きを止めたい（SSを自由にしたい） |
| 発光量を抑えて連写したい | フィルターの着脱をしたくない |
| 光量に余裕がない | 光量に余裕がある／被写体が近い |

**ND経路の弱点は、SSが同調速度に固定されること**です。走る被写体を背景ごと止めたい場合、
1/250 では足りません。そのときはHSS経路を選びます。

## HSSの光量ロスについて

HSSは発光を細かく連続させて幕の動きに追従させる方式なので、**光がかなり失われます。**
損失は「基準損失＋同調速度からの超過段数」で計算しています。基準損失の既定は2段ですが、
**機種差が大きい**ため設定タブで 1.0〜2.5 の範囲で調整できます。

実測とズレていたら調整してください。テスト撮影で「計算より1段暗い」なら基準損失を1段増やします。

---

# §7.5 <a id="help-blowout"></a>空が飛ぶのを防ぐ — 適正露出でも飛びます

## 適正に合わせても空は飛ぶ

**これがいちばん誤解されるところです。** 被写体を適正露出にしても、空はしばしば飛びます。
露出計算が間違っているのではなく、**シーンの明暗差がセンサーの記録できる幅を超えている**からです。

センサーには「中間調から白飛びまでの余裕」があります。これを**ヘッドルーム**と呼びます。

| 記録形式 | ヘッドルーム（目安） |
| --- | --- |
| JPEG | **約3段** |
| RAW | 3.5〜4段 |

つまり、被写体を適正にしたとき、**それより3段以上明るいものは白く塗り潰されます。**

快晴の逆光では空が被写体より 4.5段 ほど明るいので、
**被写体を適正にした瞬間に空は 1.5段 オーバーして飛びます。** これは避けようがありません
——**背景段数を下げる以外に。**

## だから背景段数を下げる

日中シンクロ（§6）とスローシンクロ（§8）では**背景の明るさを選べます。**
ここを下げれば空が記録範囲に入ります。

アプリはこう出します。

> 空が飛ぶ可能性があります（快晴・順光・逆光の目安 4.5段）。
> 背景 0段 → 1.5段オーバー／背景 −1段 → 0.5段オーバー／背景 −2段 → 空が残ります（推奨）。
> ※空を飛ばす表現を狙う場合はこの警告を無視してかまいません

**段数そのものではなく「選ぶべき背景段数」を出しています。** コントラストの目安は±1段の精度しか
ないので、「4.5段の差がある」と言われても判断できません。「背景を −2段 にすれば残る」なら
そのまま操作できます。

## 「逆光」トグル

**逆光かどうかはシーンEV では区別できません。** 同じ「快晴」でも、順光と逆光で
空と被写体の明暗差は2段変わります。だからシーンとは別のトグルにしています。

| | 空と被写体の差（目安） |
| --- | --- |
| 快晴・順光 | 2.5段 |
| 快晴・**逆光** | **4.5段** |
| 晴れ・薄曇り | 2.0段 |
| 晴れ・薄曇り・**逆光**（＝被写体が日陰） | **4.0段** |
| 曇り | 1.5段 |
| 厚い曇り／日陰 | 1.0段 |
| 日没直後 | 2.0段 |

**日中シンクロは逆光で使うことが多い**ので、忘れずに入れてください。
入れないと警告が出ず、飛ばしてから気づくことになります。

## 判定が出ないシーンがあります

薄暮と屋内・夜間のシーンでは**この警告を出しません。**

これらのシーンで最も明るい背景は、空ではなく**光源そのもの**（窓・街灯・空の残光）です。
被写体との差はシーンEV ではなく**どう画角を切るか**で決まるので、表では当てられません。
当てずっぽうの警告を出すより黙るほうが害が小さい、という判断です。

夜に窓や照明を背景に入れるときは、飛ぶ前提で構図を決めてください。

## 背景を下げると壁が上がります

**背景段数を下げると、日中シンクロでは同調速度の壁（§7）が高くなります。**
背景を1段暗くする＝SSを1段速める＝壁が1段上がる、という関係です。

壁が上がればND が増えるかHSS の損失が増え、**ストロボが届かなくなることがあります。**
そのためアプリは、推奨した背景段数が成立するかを確認しています。成立しないときはこう出ます。

> ただし背景 −2段 では光量が 1.5段 足りません（減光がその分増えるため）。
> 距離を詰めるか ISO を上げてください

**提案が別の問題に置き換わるだけなら、そう書きます。**

## ヘッドルームの調整

設定タブの**ハイライト余裕(段)**で 2.0〜4.5 の範囲で変えられます。既定は 3.0（JPEG 相当）。

RAW で撮って現像で持ち上げるなら 3.5〜4.0 に。
「警告が出るのに実際は飛んでいない」なら上げ、「警告が出ないのに飛ぶ」なら下げてください。

**コントラスト目安もヘッドルームも±1段程度の目安です。** 断定ではありません。

---

# §8 <a id="help-slow-sync"></a>スローシンクロの手順

暗い場所で、**背景の夜景をSSで拾いながら、主被写体をストロボで止める**撮り方です。

## 入力するもの

日中シンクロと同じ項目に加えて、

- **三脚を使うか** — 使うなら手ブレ判定を飛ばし、SSを30秒まで開放します
- **先幕／後幕** — 計算には影響しませんが、助言が変わります

## 手順

1. 背景の明るさ（既定 −1段）を決める
2. アプリがアンビエント側のSSを出す
3. 三脚なしなら手ブレ限界と比べ、割っていたらISOを上げて調整
4. ストロボ側の発光量が出る
5. **閃光時間の警告が出ていないか確認**（§9）

## 後幕シンクロについて

SSが 1/60 より遅くなると「後幕シンクロ推奨」が出ます。

先幕（シャッターが開いた瞬間に発光）だと、被写体が動いた場合に**残像が進行方向の前に伸びます。**
歩いている人の前方にブレが出るので不自然に見えます。後幕（閉じる直前に発光）なら残像が後ろに残り、
動きの流れとして自然になります。

---

# §9 <a id="help-flash-duration"></a>閃光時間 — フル発光では止まらない

## 落とし穴

スローシンクロで被写体を止めているのは、**SSではなく閃光時間**です。
ここを勘違いすると、SSをいくら調整しても被写体がブレます。

そして**発光量が大きいほど閃光時間は長くなります。**

| 発光量 | 閃光時間の目安 |
| --- | --- |
| 1/1（フル） | 1/250 |
| 1/2 | 1/400 |
| 1/4 | 1/700 |
| 1/8 | 1/1200 |
| 1/16 | 1/2000 |
| 1/32 | 1/3500 |
| 1/64 | 1/5000 |
| 1/128 | 1/9000 |

**200Wsをフル発光すると閃光時間は1/250秒程度**あります。走る人を止めるには 1/500 が必要なので、
これでは止まりません。「発光量1/1では閃光時間1/250。この被写体は止まりません」という警告が出ます。

## 対処

**距離を詰めて発光量を落とす**のが基本です。距離を半分にすれば必要な発光量は2段下がり、
閃光時間は 1/250 → 1/700 になります。

他の手段としては、ISOを上げる（発光量を下げられる）、F値を開ける（同じ）があります。
いずれも「発光量を下げる」ことが目的です。

> この表の値は機種差が大きい目安です。手持ちの機材の公称値が分かれば、そちらを信じてください。

---

# §10 <a id="help-power-mode"></a>発光量を自分で決める

## なぜ自分で決めるのか

フル発光は被写体がまぶしくて実用になりません。実際に使うのは **1/8〜1/128** がほとんどです。
また発光量が小さいほど**閃光時間が短くなって被写体が止まり**、**チャージも速くなって次のカットが撮れます**
（閃光時間の表は §9）。だから「弱く光らせて、そのぶん距離を詰める」のが基本の組み方になります。

## 2つのモード

ストロボの入力欄にある**発光量チップ**で切り替えます。

| 選択 | アプリがすること |
| --- | --- |
| **おまかせ** | 設定した距離で適正になる発光量を選ぶ |
| **1/4〜1/128** | その発光量で適正になる**距離**を計算して「推奨距離 ◯m」を出す |

自分で発光量を決めたときは、距離チップの該当する値に目印が付きます。そこまで寄る（離れる）と適正です。

**1/4 があるのは、100Ws クラスで 1/8 だと寄りすぎになるため**です。1/8 では被写体まで1m以内に
入る必要があり、広角で背景も入れたい場面では成立しません。1/4 なら倍の距離が取れます。

## 1/2 と 1/1 がチップに無い理由

閃光時間が長くなりすぎるためです（9 の表）。

| 発光量 | 閃光時間 | 止まる被写体 |
| --- | --- | --- |
| 1/4 | 1/700 | 走る人（1/500）まで止まる |
| 1/2 | 1/400 | 歩く人（1/250）が限界 |
| 1/1 | 1/250 | 静物のみ |

**1/2 と 1/1 は被写体ブレの領域**なので、自分で選ぶ対象から外しています。
どうしても必要な場合は「おまかせ」にすれば、光量が足りなければアプリがそこまで使います。

## 距離が計算される理由

発光量を決めたあと、アプリが動かすのは**距離だけ**です。F値やISOを動かさないのには理由があります。

**距離はストロボにしか効きません。** カメラから離れた場所にあるストロボを1m動かしても、
背景の明るさは変わりません。一方 F値やISOを動かすと**背景まで一緒に変わり**、
せっかく決めた「背景を−1段暗く」という設定が崩れてしまいます。

> 距離だけが、背景に触らずにストロボの明るさを変えられる唯一の入力です。

## 発光量と距離の両方を決めたとき

どちらも固定すると、条件が1つ多すぎて計算が合わないことがあります。
そのときアプリは**勝手にどちらかを変えず**、ずれを段数で伝えます。

> 発光量 1/32 では 1.9段 強すぎます。2.9m まで下がる／F を 1.9段絞る／ISO を 1.9段下げる

**読み方：** 「その発光量なら 2.9m が適正なのに、いま 1.5m に設定されているので 1.9段ぶん明るすぎる」
という意味です。3つの案はどれを選んでも成立します。

| 案（強すぎるとき） | 効果と副作用 |
| --- | --- |
| 表示された距離まで下がる | 段数ぶん暗くなる。**背景の明るさは変わらない**ので最も安全 |
| F を◯段絞る | ストロボも背景も同じだけ暗くなる（背景段数が変わる） |
| ISO を◯段下げる | 同上。背景も暗くなる |

足りないときは逆向きです（距離を詰める／F を開ける／ISO を上げる）。

3つのうちどれを選ぶかは撮影者の判断です。背景の明るさを変えたくなければ距離を選んでください。

## <a id="help-power-ceiling"></a>「おまかせ」でも上限がある

設定タブのプロファイルに**発光量の上限**（既定 1/4）があります。おまかせでも、これより強い発光量は選びません。
上限では光が足りない場合、黙って上限で止めずに不足を伝えます。

> 上限 1/4 では 1.4段 足りません。上限を 1/1 に上げる（発光量 1/2 で撮れます）／距離を 2.0m まで詰める／ISO を 1.4段上げる

**この警告が出たときの読み方：** 「いまの距離では、上限の発光量でも 1.4段ぶん光が足りない」という意味です。

### 第一候補は「上限を上げる」

**上限は機材の限界ではなく、あなたが決めた好みです。** 閃光時間を短く保つために弱めに
制限しているだけなので、いちばん簡単な解決は上限を戻すことです。

警告の下にボタンが出ます。**押すとその場で上限が変わります。** 設定タブへ移動する必要はありません。

### 「上限 1/1」と「発光量 1/2」は矛盾していません

上に出る2つの数は別のものです。**取り違えやすいので必ず両方を出しています。**

| 数 | 意味 |
| --- | --- |
| 上限 **1/1** | 「ここまで強くしてよい」という線。あなたが決める設定値 |
| 発光量 **1/2** | その線の中でアプリが実際に選ぶ値 |

上限を 1/1 に開けても、必要なのが 1/2 ぶんなら 1/2 で撮ります。
**線を 1/2 に置くのでは足りません。** 必要量が「1/1 と 1/2 の間」にあるとき、
線を 1/2 に置くと 1/2 より強い側が使えず、まだ届きません。
だから線は 1/1 まで開ける必要があります。

警告に出る上限値は、**この意味で必要量を満たす最も弱い線**です。
それより弱い線では届かず、それより強く開ける必要もありません。

| 案 | 向いている場面 |
| --- | --- |
| 上限を上げる | ほとんどの場合これ。実際に使う発光量のぶん閃光時間が長くなるので、動く被写体では次の案を検討 |
| 距離を詰める | 被写体に寄れるとき。**背景の明るさは変わらない**ので最も安全 |
| ISO を上げる | 寄れないとき。背景も同じだけ明るくなる（背景段数が変わる） |

### 「ND を外す」はこの警告では出しません

ND はレンズ前にあるので**アンビエントとストロボの両方に等しく効きます。**
ストロボが足りないからと ND を外すと、背景が同じだけ明るくなり「明るすぎます」に戻ります。
その対処でまた ND を足す——という往復になります。

```
背景が明るすぎる → ND を足す
  → ストロボが上限を超える
    → ND を外す
      → 背景が明るすぎる（元に戻る）
```

**アプリは、適用しても警告が減らない案を出しません。** ND を外す案は、
背景側に明るくなる余地が残っているときだけ出ます。

上限を最強（1/1）まで上げても届かない場合は「**この機材では届きません**」に切り替わります。
このときは距離・ISO・F値で稼ぐか、より大きい機材が必要です。

（「最小発光量」は**弱い側**の限界、「発光量の上限」は**強い側**の限界です。混同しないよう注意）

---

# §11 <a id="help-nd"></a>NDフィルターの使い方

## 何段必要かは自動で出ます

必要な減光段数から、**手持ちのNDで作れる組み合わせ**をアプリが探します。
枚数が最少になる解を優先します。

| 必要段数 | 組み合わせ（ND2/4/8/16 所有時） |
| --- | --- |
| 1〜4段 | 1枚で足りる |
| 5〜7段 | 2枚 |
| 8〜9段 | 3枚 |
| 10段 | 4枚全部 |
| 11段以上 | 所有NDでは不可 |

所有しているフィルターは設定タブで変更できます。買い足したら追加してください。

## 枚数を減らしたい理由

ガラスを重ねるほど**ケラレ・色被り・フレア**が増えます。特に広角では周辺が暗くなります。
3枚以上になると警告が出ます。

## 2枚必要になったときの代案 — 拡張ISO トグル

ベースISO200のカメラで5段必要になると、ND2+ND16 の2枚重ねになります。
このとき **「拡張ISO（50〜）を使えば ND 1枚で済みます」** という案が出ます。

ND チップの下にある **「拡張ISO（50〜）を使う」** トグルを入れると、ISOの下限が
ベースISO（200）から拡張下限（50）まで広がります。**ISO を2段下げられるぶん、
必要な ND が2段減ります。**

| トグル | ISOの下限 | ND |
| --- | --- | --- |
| OFF（既定） | ベースISO 200 | 多く必要（例：5段＝2枚） |
| ON | 拡張下限 50 | 少なくて済む（例：3段＝1枚） |

**ガラスを重ねるか、拡張ISOの画質低下を取るかのトレードオフです。**
拡張ISOは階調やノイズで不利になる場合があります。**アプリは両方を出すだけで、選びません。**

トグルを入れている間は「拡張ISOを使えば…」の案内は出ません（すでに使っているため）。
かんたんタブと計算タブの両方に同じトグルがあり、どちらから変えても連動します。

## NDはストロボにも効く

§5 のとおりです。ND4（2段）を付けたら、ストロボの発光量も2段上げる必要があります。
アプリは自動で計算に入れているので、表示された発光量に従ってください。

## 減光しないフィルター（ブラックミスト）

ブラックミストは**露出をほとんど変えません**（減光0段として扱っています）。
NDチップの右側に、破線の枠で区切って置いてあります。減光段数の合計には影響しません。

実測で微小な減光がある場合は、設定タブの「ブラックミスト減光(段)」で 0〜0.33段の範囲で入れられます。
値を入れると、NDと同じように**アンビエントとストロボの両方**に効きます（レンズ前にあるため）。

**ただし枚数には数えます。** 減光しなくてもレンズ前のガラス1枚なので、
ケラレや周辺光量落ちのリスクはNDと同じです。ND2枚＋ブラックミストで3枚になると警告が出ます。

> フィルター3枚（ND2+ND16 + ブラックミスト）。24mm では周辺が落ちる可能性があります

常用焦点距離が広角のときは特に注意してください。

---

# §12 <a id="help-calibration"></a>ストロボの校正 — 精度を上げる

## なぜ校正が必要か

**Ws（ワット秒）は蓄積エネルギーであって、光の量ではありません。**
実際の明るさはリフレクター、モディファイア、発光管の効率で大きく変わります。
メーカーの公称ガイドナンバーも測定条件がバラバラで、そのままでは使えません。

そこでアプリは `GN = k × √Ws` という推定式を使い、**係数 k をテスト撮影1回で実測に合わせます。**
既定の k は 4.0（100Ws で GN40、200Ws で GN57）ですが、実際は 2.5〜3.5 に落ち着くことが多いです。

未校正のプロファイルには **「推定値」バッジ**が出ます。これが消えるまではズレる前提で使ってください。

## やり方（5分）

1. ストロボを**標準リフレクター**（またはいつも使うモディファイア）で設置
2. 被写体までの距離を測る。**3m 前後が測りやすい**
3. 発光量を **1/1（フル）** に設定
4. ISOを **100** に設定
5. F値を変えながら撮り、**ちょうど適正になったF値**を見つける
6. 設定タブ →「テスト撮影で校正」に、距離・F値・ISO・発光量を入力

これだけで k が更新され、以降すべての計算が実機に合います。

## 例

200Ws、距離3m、ISO100、フル発光で **F11 が適正**だった場合、

```
実測GN = 11 × 3 = 33
k = 33 ÷ √200 = 2.33
```

既定の 4.0 から 2.33 に下がりました。**約1.5段ぶん**の差です。
校正前は「到達5.0m」と出ていたものが実際は3.0m だったことになります。

## モディファイアごとに校正する

校正値は**プロファイルごと・モディファイアごとに保存**されます。
例えば 200Ws × 標準リフレクターで校正しても、**200Ws × オクタ60cm は未校正のまま**です。
切り替えると「推定値」バッジが戻り、その組み合わせを校正すると消えます。

| 組み合わせ | 状態 |
| --- | --- |
| 200Ws × 標準リフレクター | 校正済み（k を実測値で保持） |
| 200Ws × オクタ60cm | 未校正（推定 k ＋ オクタの減光2段で計算） |

オクタ60cm と オクタ90cm を両方使うなら、それぞれ別に校正してください。

よく使う組み合わせから順に校正していけば十分です。

---

# §13 計算タブの使い方

## 2つ決めて3つ目を出す

F値・SS・ISO のうち**2つの鍵アイコンを閉じる**と、残り1つが自動で計算されます。
鍵を開けて別の組み合わせに変えれば、そちらが計算されます。

シーンのEVはかんたんタブから引き継がれます。直接入力もできます。

## グレーになっている目盛り

ダイアルの端のほうが**グレー**になっていることがあります。ここは**設定した機材では
使えない範囲**です。区切り線から先へは動きません。F値なら開放と最小絞り、
SSならカメラの最高速シャッター、ISOなら下限です。値は消さずに残してあるので、
どこが限界なのかが見えます。動かそうとすると理由が短く出ます。

グレーの範囲は**設定タブ**で変えられます。別のレンズに付け替えたときは開放F値と
最小絞りを、ISOの下限を下げたいときは**拡張ISO**を使ってください
（拡張ISO は計算タブにもトグルがあります）。

**ISO の上は止めていません。** ノイズが出ても上げたい場面はあるので、実用ISO上限を
超えても選べます。上限に近づくと「ノイズが目立ち始める領域です」と出るだけです。

なお、設定を変えたときに選んでいた値が範囲から外れると、範囲内に寄せたうえで
「ISO を 200 に変更しました（拡張ISO が無効のため）」のように知らせます。
黙って変えることはありません。

## 等価露出の一覧

下部に**同じ明るさになる組み合わせ**が3つ並びます。**F2.8 / F5.6 / F11 の2段刻み**です。

| 行 | 使いどころ |
| --- | --- |
| F2.8 | 背景をぼかす |
| F5.6 | 標準 |
| F11 | パンフォーカス |

3つに絞ってあるので、結果パネル内でスクロールせずに読めます。

**開放F値がその行より暗いレンズ**（例：開放F4.0）では、F2.8 の行は
「開放F4.0のため使用不可」と表示されます。行は消えません。位置が動かないほうが読みやすく、
なぜ使えないのかも分かるためです。

各行に小さな警告アイコンが付きます。

| アイコン | 意味 |
| --- | --- |
| 手ブレ | この行のSSでは手ブレしやすい |
| 回折 | F値が絞りすぎで解像が落ちる |
| 高感度 | ISOが実用上限に近い |

**アイコンが付いていない行から選ぶのが安全です。** 現在の主案の行はハイライトされます。

## <a id="help-calc-clamp"></a>「F が 1段 足りません」と出たとき

計算タブでは、鍵を閉じた2つを優先して残り1つを計算します。
その計算結果が**レンズやカメラの限界を超えた**とき、この警告が出ます。

> F が 1段 足りません。ND を1枚外す／ロックを外して SS を 1段遅くする／ISO を 1段上げる

例えば ISO と SS を閉じた状態で、必要なF値が F2.0 と出たのに、レンズの開放が F2.8 だとします。
このときアプリは **F2.8 で止めて（クランプして）表示し、1段足りないことを伝えます。**

**鍵を勝手に開けません。** 鍵はあなたの意思表示なので、アプリの判断で外すことはしません。
そのかわり、何をどれだけ動かせば成立するかを段数で出します。

| 出る条件 | 意味 |
| --- | --- |
| F が◯段 足りません | 計算結果が開放F値より明るい側。レンズが対応できない |
| 明るすぎます（◯段超過） | 計算結果が最小F値より暗い側、SSが最速を超える、ISOが下限を割る |

ズレが **1/3段より小さいときは警告を出しません。** 表示は1/3段刻みなので、
それ未満のズレは表示上の丸めに埋もれて意味を持たないためです。

---

# §14 <a id="help-warnings"></a>警告の読み方

警告には3段階あります。**色だけでなくアイコンと文言でも区別**しています。

| レベル | 意味 | 対応 |
| --- | --- | --- |
| **情報**（info） | 知っておくとよいこと | 対応不要。読み流してよい |
| **注意**（warn） | 撮れるが品質に影響する | 許容できるか判断する |
| **警告**（alert） | このままでは撮れない | 対応が必要 |

## 「○段」の意味

警告には必ず**何段足りない／超過しているか**が入ります。これは対処の量を示しています。

- 「2段足りません」→ ISOを2段上げる、F値を2段開ける、距離を半分にする、のいずれか
- 「5段超過」→ 5段の減光が必要

**段数が分かれば、どの手段を何段分使うかを自分で組み立てられます。**
「足りません」だけでは動けませんが、「2段」なら判断できます。

段数は**整数に丸めません。** 「4.7段」と出たら本当に 4.7段 必要です。
4段のND（ND16）を付けても 0.7段 足りず、警告は消えません。

## 1/3段より小さいズレは出しません

露出の過不足を見るすべての警告に、**1/3段の足切り**があります。

表示は1/3段刻みです。それより小さいズレはそもそも表示上動かせないので、
警告されても対処のしようがありません。

この足切りが要るのは、3つの刻みが噛み合わないためです。

| 要素 | 刻み |
| --- | --- |
| F値・SS・ISO の表示 | 1/3段 |
| NDフィルター | 1段 |
| シーンEVと目標露出 | 連続値（端数あり） |

端数は必ずどこかに残ります。**残った端数に警告を出すと、何をしても消えません。**
そのため 1/3段 未満は「合っている」とみなします。

逆に、**0.5段のズレは消しません。** 1/3段刻みで動かせる量なので、対処できるからです。

## 減光が必要なときは組み合わせまで出します

「明るすぎます」の警告には、**所有しているNDから作れる組み合わせ**が付きます。

> 明るすぎます。4.7段の減光が必要です。ND2+ND16（5段）を装着してください

NDは1段単位でしか存在しないので、**必要量を切り上げた組み合わせ**を出します。
4.7段 に対して 5段 を勧めるのは、切り捨てると足りずに警告が残るためです。
切り上げて 0.3段 濃くなったぶんは、アプリが自動計算している軸（ISOなど）が吸収します。

所有NDで足りないときは「所有 ND（合計◯段）では ◯段 に届きません」と出ます。
設定タブの**所有ND**を実際に持っているものに合わせておいてください。

## <a id="help-warn-shake"></a>手ブレしやすい速度です

SSが手ブレ限界（1÷焦点距離×補正段数）より遅い状態です。
三脚を使う、ISOを上げてSSを稼ぐ、焦点距離を短くする、のいずれかで解決します。

**不足が1/3段未満のときは警告を出しません。** 1÷焦点距離の法則自体が±1段程度の精度しかなく、
わずかな不足で警告するのは意味のある精度ではないためです。

## <a id="help-warn-motion"></a>被写体が流れます

被写体の動きに対してSSが足りません。表示された速度以上にしてください。
流し撮りなど**意図的にブレさせたい場合は無視してかまいません。**

## <a id="help-warn-light-short"></a>光量が足りません

ISOを上限まで上げ、F値を開放まで開けても明るさが足りない状態です。
三脚を使ってSSを稼ぐ、より明るいレンズを使う、ストロボを使う、のいずれかが必要です。

実用ISO上限は設定で変えられます。ノイズを許容できるなら上げてください。

### 「暗すぎます。SS は 30″ が限界で◯段 足りません」

SSの限界（30秒）でも足りない状態です。**ND を足しすぎたときにも出ます。**

必要量が 4.7段 のときアプリは 5段 の組み合わせを勧めますが、
所有NDの刻みによっては 7段 しか作れないことがあります。このとき 2.3段 暗くなり、
自動計算している軸では吸収しきれません。組み合わせの提案に
**「（2.3段 暗くなります）」**と付く場合は、この状態になることを先に知らせています。

明るすぎ側と同じく、**1/3段未満のズレでは出しません。**

## <a id="help-warn-diffraction"></a>回折で解像が落ち始めます

F11 を超えると、絞りすぎによる回折で全体の解像が下がります。
被写界深度を稼ぐために必要な場合はそのまま撮ってかまいません。**情報レベルの通知です。**

## <a id="help-warn-high-iso"></a>ノイズが目立ち始める領域です

実用ISO上限の半分を超えました。撮れますが、暗部のノイズが見えてきます。

## <a id="help-warn-tripod"></a>三脚推奨

SSが1/2秒以上です。手持ちでは厳しい速度です。

## <a id="help-warn-flash-strong"></a>光が強すぎます

最小発光量まで下げても被写体が明るすぎる状態です。
**距離を離す**（2倍離せば2段下がる）、ISOを下げる、F値を絞る、NDを追加する、のいずれかで解決します。

## <a id="help-warn-flash-short"></a>ストロボが足りません／光が届きません

フル発光でも光量が足りない状態です。表示された段数ぶん、以下のいずれかで稼ぎます。

| 手段 | 効果 |
| --- | --- |
| 距離を半分にする | 2段 |
| ISOを2倍にする | 1段 |
| F値を1段開ける | 1段 |
| NDを外す | ND の段数ぶん |
| モディファイアを外す | 1〜2.5段 |

**距離が最も効きます。** 6mから3mに詰めるだけで2段稼げます。

## <a id="help-warn-recycle"></a>チャージが追いつきません

発光量が1/2以上です。フル発光に近いとチャージに時間がかかり、連写では発光しない駒が出ます。
1コマずつ撮るか、距離を詰めて発光量を下げてください。

---

# §15 <a id="help-offline"></a>オフラインで使う

## ホーム画面に追加する

Safari で開いて、共有ボタン →「ホーム画面に追加」。
これでアプリのように起動し、**電波がなくても全機能が動きます。**

追加せずブラウザで使っても動きますが、アドレスバーで画面が狭くなります。

## 追加したら、電波のある場所で一度起動する

**ホーム画面に追加しただけでは、まだオフラインで起動できません。**

ホーム画面のアプリは自分専用の保存領域を持ちます。追加した直後はそこが空なので、
一度起動して必要なファイルを端末に保存する必要があります。
このとき電波が無いと保存できず、次に開いても起動しません。

追加したあと、電波のある場所で一度起動してください。
画面下に **「オフラインで使えるようになりました」** と出れば準備完了です。
以降は機内モードでも地下でも山でも起動します。

この通知は最初の一度だけです。二度目以降の起動では出ません。

> **現場に着いてから追加しても間に合いません。** 電波の無い場所で追加すると、
> 起動しても白い画面のままになります。**出発前に、家か車の中で一度起動しておいてください。**

## 通信について

このアプリは**一切通信しません。** 計算はすべて端末内で行われ、入力した内容はどこにも送られません。
機内モードでも通常どおり使えます。

## 更新のされかた

新しいバージョンが用意されると、次にオンラインで開いたときにヘッダーへ
**「新しいバージョンがあります ［更新］」** というバーが出ます。

**押すまで更新されません。** 計算中に画面が勝手に切り替わることはありません。
現場では押さず、帰ってから押してかまいません。

バーが出ていないときは、更新が無いか、まだ検出されていない状態です
（初回インストール時にも出ません）。オフラインのままでは検出されないので、
一度電波のある場所で開いてください。

## 設定の保存先

機材設定や校正値は**端末内**に保存されます。バックアップも同期もされません。

- ブラウザの履歴やサイトデータを消すと設定も消えます
- プライベートブラウズでは保存されません（計算は使えます）
- 別の端末には引き継がれません

校正値は入れ直すのに時間がかかるので、**k の値を手元にメモしておく**と安心です。

---

# §16 <a id="help-troubleshoot"></a>こんなときは

## 計算結果が実際とズレる

まず**シーンの微調整**を確認してください。シーンEVは±1段の目安です。
1段暗かったなら微調整を +1.0 にすれば以降は合います。

ストロボがズレる場合は**校正**（§12）をしてください。未校正だと1〜1.5段ズレることがあります。

## ストロボの到達距離が実際より長く出る

未校正の可能性が高いです。「推定値」バッジが出ていないか確認してください。
§12 の手順で校正すると合います。

## HSSにすると計算より暗い

HSS基準損失が実機と合っていません。設定タブで値を上げてください（既定2.0、範囲1.0〜2.5）。
「1段暗い」なら基準損失を1段増やします。

## 設定が保存されない

プライベートブラウズになっていないか確認してください。
画面に「設定を保存できません」と出ている場合、計算機能は使えますが設定は毎回初期値に戻ります。

## 更新バーが出ない

更新は**オンラインで開いたとき**に検出されます。オフラインのままでは出ません。
一度電波のある場所で起動してください。

## 画面が白いまま／数字が出ない

ホーム画面のアイコンを長押しして削除し、Safari から開き直して再度追加してください。
設定は残ります（同じ保存領域を使うため）。

## 入力した数値が勝手に変わる

範囲外の値は自動で範囲内に丸められます。「8〜1200mm の範囲に丸めました」のような通知が出ます。
入力を拒否せず丸める設計です。
