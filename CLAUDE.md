# CLAUDE.md

## このプロジェクト

撮影現場で適正露出を求める PWA。シーンと撮影意図から F値・SS・ISO・ストロボ発光量を逆算し、
トレードオフを警告する。**日中シンクロ／スローシンクロが主要機能**であり、おまけではない。

素の HTML / CSS / JS のみ。ビルドステップなし。GitHub Pages でホストし、iPhone のホーム画面から
オフラインで使う。

## 仕様書（正典）

作業開始時に**必ず両方を読むこと。**

| 文書 | 範囲 |
| --- | --- |
| `docs/exposure-app-spec.md` | 計算仕様・データ・ロジック・受け入れテスト |
| `docs/exposure-app-ui-spec.md` | 画面構成・コントロール・トークン・アクセシビリティ |

**矛盾したときは計算仕様を優先する。** この CLAUDE.md は両者を要約しない。
不変条件と作業手順だけを持つ。詳細は必ず仕様書を参照すること。

## 質問してよい／いけない

仕様書は判断が必要な点をすべて確定済み。**質問せず通しで実装してよい。**
仕様間の矛盾か、物理的に実装不能な記述を見つけたときだけ確認すること。

未確定だったアプリ名は**「露出アシスト」**で確定（`manifest.webmanifest` の `name` / `short_name`）。
実機で確定した既定値の上書きは `js/state.js` 冒頭のコメントを参照。

---

## 絶対に破ってはいけない制約

### 配布

- **外部リソース参照ゼロ。** CDN・Web フォント・画像 API・アナリティクス、一切禁止。オフラインで死ぬ
- **すべて相対パス。** GitHub Pages のプロジェクトサイトは `/<repo>/` 配下。絶対パス `/` は 404
- `manifest.webmanifest` の `start_url` と `scope` は `"./"`
- `sw.js` はルート直下。`register('./sw.js', { scope: './' })`
- `index.html` はルート直下
- npm・バンドラ・トランスパイラを導入しない。ES Modules をそのまま使う

### ドメインの物理（間違えると全体が壊れる）

- **シャッター速度はストロボ露出に影響しない。** 発光時間が SS より短いため。テスト #26 がこれを守る
- **ND はアンビエントとストロボの両方に等しく効く。** レンズ前にあるため。テスト #27 がこれを守る
- **1/3段への丸めは表示直前の一度だけ。** 途中で丸めると誤差が累積する
- **表示ラベルと厳密値は一致しない。** `15″` の実体は 16 秒、`F5.6` は 5.657。丸めは厳密値で行い、ラベルは対応表から引く
- **距離は「ストロボ→被写体」であってカメラからではない。** オフカメラ運用が前提
- **Ws から GN は一意に決まらない。** 推定値として扱い、校正機能を必ず実装し、未校正なら「推定値」バッジを出す
- **ND 経路と HSS 経路は両方計算して並べる。** どちらかを自動選択しない
- **発光量が大きいほど閃光時間は長い。** フル発光では被写体が止まらない

### UI

- 通常操作でソフトキーボードを出さない（「その他」入力と設定タブのみ例外）
- 結果パネルはどのスクロール位置でも見えている
- 範囲外の入力を拒否しない。クランプして通知する
- 入力欄の `font-size` は 16px 以上（下回ると iOS が自動ズームする）
- タップ対象は 44×44px 以上、間隔 8px 以上
- 絵文字をアイコンとして使わない。インライン SVG の `<symbol>` を自前で定義する
- コンポーネント内に生の hex を書かない。CSS カスタムプロパティ経由のみ
- `--flash` トークンはストロボ系統の識別にのみ使う。UI 全体の強調色は `--accent`
  （初期値は同値。ストロボの色を変えてもフォーカスリングまで変わらないよう分離してある）
- ライトとダークのコントラストは**それぞれ実測**する。片方から推測しない
- **連続的・スクロール駆動のアニメーションは `transform` と `opacity` に限る**
  （レイアウトとペイントを避ける）。離散操作による一度きりの遷移では、ペイントのみの
  プロパティ（`background-color` / `border-color` / `box-shadow`）を使ってよい。
  レイアウトを動かすプロパティ（`height` / `max-height` / `width` / `margin`）は、
  **実機で滞りがないことを確認し MAINTENANCE.md §12 に記録した場合のみ**

---

## アーキテクチャ

```
js/stops.js  exposure.js  flash.js  filters.js  advisor.js  scenes.js   ← 純粋関数・定数のみ
                          │
                   js/compute.js（compute(state) → derived）            ← 純粋。束ねる層
                          │
state ──▶ compute(state) ──▶ derived ──▶ render(state, derived) ──▶ DOM
  ▲            ▲                              │                       │
  │       js/state.js（既定値・migrate）  js/wheel.js（DOM部品）        │
  └───────────── setState(patch) ◀───── イベントハンドラ ◀───────────┘
                     │
              js/storage.js（永続化。setState の直後1箇所）
```

- 計算モジュール（`stops` / `exposure` / `flash` / `filters` / `advisor` / `compute` / `scenes` / `state`）は
  **副作用ゼロ**。DOM にも `localStorage` にも `window` にも触らない
- **意図別ロジック・警告の組み立ては `compute.js`。** `advisor.js` は単発の判定と文言整形を持つ。
  タブによる分岐は `compute()` の入口の1箇所だけに置く
- `ui.js` に計算を書かない。`compute()` の結果を描画するだけ
- `wheel.js` は状態を持たない DOM 部品。操作用（計算タブ）と表示用（結果パネル）で共有する
- **DOM を状態の保管場所にしない。** `element.value` を読んで判断しない。必ず `state` を見る
- 再描画経路は `setState` の一本だけ。他に作らない
- 永続化は `setState` の直後に1箇所で行う（アプリ状態 `expo-state-v1` と
  一度きりのフラグ `expo-flags-v1` は別キー。詳細は MAINTENANCE.md §9）

---

## 実装順序

**純粋関数のテストが全部緑になるまで UI に触らないこと。** 物理を DOM 越しにデバッグすると必ず沼る。

1. `stops.js` — 1/3段テーブルの生成と丸め → テスト #30, #31
2. `exposure.js` — アンビエントのソルバー → テスト #1〜#9
3. `filters.js` — ND の組み合わせ探索 → テスト #15〜#18
4. `flash.js` — Ws→GN・校正・HSS・閃光時間 → テスト #10〜#14
5. `advisor.js` — 単発の判定・文言整形・日中シンクロの経路計算 → テスト #19〜#29
5b. `compute.js` — 意図別ロジック・警告の組み立て・タブ分岐 → 末尾 c のテスト（#24c 等）
6. **ここで `tests.html` が全緑になることを確認する**
7. `index.html` の骨格＋ CSS トークン＋ SVG シンボル
8. コンポーネント（ホイールピッカー → チップ → タイル → 結果パネル → EV ルーラー）
9. `storage.js` と初回セットアップ
10. `manifest.webmanifest` と `sw.js`
11. 納品前チェックリスト（UI 仕様書 §16）の自己検証

各段階の完了時にコミットする。7 以降に入ってから計算を直したくなったら、
まず `tests.html` にケースを足してから直す。

---

## 動かし方

```bash
# ローカル開発（Service Worker は localhost なら HTTPS 不要）
python3 -m http.server 8000

# テスト
open http://localhost:8000/tests.html
```

`tests.html` は各ケースの `期待値 / 実測値 / 合否` を表で出し、
最上部に `全 N 件中 M 件成功` を表示すること。失敗行は `--alert` 色で示す。

---

## この題材で起こしやすい間違い

実装中に以下をやりかけたら止まること。

| やりがちな間違い | 正しくは |
| --- | --- |
| F値の 1/3段配列を `[1.4, 1.6, 1.8...]` とベタ書き | `2 ** (k/6)` で生成し、ラベルは別配列 |
| SS を変えたらストロボの発光量も再計算 | ストロボ側は SS に依存しない |
| ND をアンビエントだけに適用 | 両方に適用する |
| 各ステップで `snap()` してから次の計算へ | 連続値のまま通し、最後に一度だけ丸める |
| Ws から GN を確定値として扱う | 推定値。校正 UI とバッジをセットで出す |
| 同調速度超過を検出したら HSS を自動適用 | ND 経路と両方を計算して並べ、ユーザーに選ばせる |
| スローシンクロで SS だけ見て「止まる」と判定 | 発光量に対応する閃光時間で判定する |
| 警告文が「ISO が足りません」 | 「ISO が 2 段足りません」。必ず段数を出す |
| 絶対パス `/css/style.css` | `./css/style.css` |
| 新しい SW を検出したら自動リロード | 更新バーを出し、押されたときだけリロード |
| `type="number"` で数値入力 | `type="text" inputmode="decimal"` |
| 警告を色分けだけで区別 | アイコン＋文言を必ず併記 |

---

## 完了の定義

- **受け入れテストが全件緑**（`tests.html` の最上部が `全 N 件中 N 件成功` になる）。
  **ここに件数を書かない。** 仕様 §12 の 31 ケースに、compute 経由の統合テストと
  実機で見つかった不具合の再発防止ケースが継ぎ足されていくので、数値を書くと必ず古くなる。
  「いま何件か」は `tests.html` を開けば分かる
- 検証ツール3本（`check-modules` / `check-sw-assets` / `check-help-anchors`）が exit 0
- UI 仕様書 §16 のチェックリストを全項目自己検証し、結果を報告済み
- 機内モードで全機能が動く。DevTools の Network に外部ドメインへのリクエストが1件も無い
- 320 / 375 / 430 / 768px と横向きで崩れない
- ライトとダークの両方でコントラストを確認済み
- キーボードだけで全操作できる

## コーディング規約

- 関数には JSDoc で引数と戻り値の型を書く
- マジックナンバーを直書きしない。定数は `scenes.js` か各モジュール冒頭
- 同じ処理を二度書きそうになったら関数に切り出す。冗長な分岐は表引きに置き換える
- `storage.js` は必ず `try/catch` で包む。失敗しても計算機能は落とさない
- 近似モデル（Ws→GN・HSS 損失・モディファイア減光・閃光時間・シーン EV）を使う箇所は、
  UI に「目安である」旨と調整手段を必ず併記する。断定的な数値を出すと現場で外れる
